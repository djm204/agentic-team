And, this pattern will be followed for all the other Python files needing tests. 

Once all the test files are written, I will execute the tests using pytest and collect the test execution results, coverage report, and pass/fail status. 

Here is an example command for running pytest and generating a coverage report: